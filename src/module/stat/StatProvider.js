const mock = require('../../mock/stats');

function getAll() {
    const listStats = mock;

    return listStats;
}

function update() {
    const stat = mock;

    return user;
}

module.exports = {
    getAll: getAll,
    update: update,
};