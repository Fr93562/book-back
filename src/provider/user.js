const mock = require('../mock/user');

function get() {
    const user = mock;

    return user;
}

function update() {
    const user = mock;

    return user;
}

module.exports = {
    get: get,
    update: update,
};